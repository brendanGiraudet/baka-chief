@inherits Fluxor.Blazor.Web.Components.FluxorComponent

@if (RecipsState.Value.IsLoading)
{
    <Loader />
}
else
{
    <div>

        <div>
            <CustomInputSearch OnInputCallback="UpdateRecipSearchTerm" @bind-Value="searchTerm" />
        </div>

        @if (RecipsState.Value.Items == null || RecipsState.Value.Items.Count() == 0)
        {
            <div>vide</div>
        }
        else
        {
            <div class="recips">
                @foreach (var recip in RecipsState.Value.Items)
                {
                    <CardWithImage Title="@recip.Name" ImageUrl="@recip.ImageUrl" OnClickCallback="async () => await OnTagClick(recip)" />
                }
            </div>
        }

    </div>
}