apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: baka-chief-certificate-claim
  name: baka-chief-certificate-claim
  namespace: {{ .Values.applicationName }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 50Mi
  storageClassName: ""
  volumeMode: Filesystem
  volumeName: baka-chief-certificate-pv
status:
  accessModes:
  - ReadWriteOnce
  capacity:
    storage: 50Mi
  phase: Bound
